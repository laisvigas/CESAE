"use strict";
//Crie um algoritmo em TypeScript para os seguintes problemas:
// a. Apresentar o produto de dois números inseridos pelo utilizador.
/*

let firstNumber : number = 0;
let secondNumber : number = 0;
let total : number = 0;

firstNumber = Number(prompt('Please, enter the first number: '))
secondNumber = Number(prompt('Please, enter the second number: '))
total = firstNumber * secondNumber;
alert(`Number ${firstNumber} * ${secondNumber} = ${total}`);

// ______________________________________________________________________________________________________
// b. Calcular a média de três números inteiros inseridos pelo utilizador.
let firstNumber : number = 0;
let secondNumber : number = 0;
let thirdNumber : number = 0;
let total : number = 0;

firstNumber = Number(prompt('Please, enter the first number: '))
secondNumber = Number(prompt('Please, enter the second number: '))
thirdNumber = Number(prompt('Please, enter the third number: '))
total = (firstNumber + secondNumber + thirdNumber) / 3;
alert(`The average value of ${firstNumber}, ${secondNumber} and ${thirdNumber} is ${total}`);

// ______________________________________________________________________________________________________
// c. Mostrar o resultado da divisão inteira entre dois números inseridos pelo utilizador.
let firstNumber : number = 0;
let secondNumber : number = 0;
let total : number = 0;

firstNumber = Number(prompt('Please, enter the first number: '))
secondNumber = Number(prompt('Please, enter the second number: '))
total = Math.floor(firstNumber / secondNumber);
alert(`The integer division of ${firstNumber} by ${secondNumber} is ${total}`);

// ______________________________________________________________________________________________________
// d. Ler um número inteiro e apresentar o seu antecedente (número anterior) e o seu sucessor (posterior).
let userNum : number = 0;
let predecessor : number = 0;
let successor : number = 0;

userNum = Number(prompt('Please, enter a number: '))
predecessor = userNum - 1;
successor = userNum + 1;
alert(`The predecessor of ${userNum } is ${predecessor} and the successor is ${successor}`);

// ______________________________________________________________________________________________________
// e. Obter a área de um triângulo.
let base: number = Number(prompt('Enter the base of the triangle:'));
let height: number = Number(prompt('Enter the height of the triangle:'));

alert('Please enter valid positive numbers for base and height.');

let area: number = (base * height) / 2;
alert(`The area of the triangle with base ${base} and height ${height} is ${area}`);

// ______________________________________________________________________________________________________
// f. Determinar o valor total a pagar de uma dada mercadoria sendo que ao valor lido deverá ser acrescido 25% de IVA.
let basePrice: number = Number(prompt('Enter the base price of the goods:'));
let vatAmount: number = basePrice * 0.25;
let totalPrice: number = basePrice + vatAmount;

alert(`The total price including 25% VAT is ${totalPrice}`);


// ______________________________________________________________________________________________________
// g. Calcular o troco efetivo a retribuir a um cliente que decide dar como gorjeta ao funcionário 10% do valor do troco inicial.
let amountPaid: number = Number(prompt('Enter the amount paid by the customer:'));
let purchaseAmount: number = Number(prompt('Enter the total purchase amount:'));

let initialChange: number = amountPaid - purchaseAmount;
let tip: number = initialChange * 0.10;
let effectiveChange: number = initialChange - tip;

alert(`The initial change was ${initialChange}. The change to return is ${effectiveChange}.`);
*/
